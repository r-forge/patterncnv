\name{scanMultiCovg}
\alias{scanMultiCovg}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
%%  ~~function to do ... ~~
Scan exome coverage of multiple samples
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
This function summarizes exon-level coverage for multi samples defined by given patternCNV session information.
}
\usage{
scanMultiCovg(session.name, sample.type = NULL, bin.size = 10, 
                     is.verbose = TRUE, is.plot = FALSE)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{session.name}{ an object of \code{\link{PatCNVSession-class}}, which is generated by \code{\link{createSession}} }
  \item{sample.type}{ a character vector of sample type defined in sample information file: e.g. "Germline" or "Somatic". A value of NULL instructs the function to select all the samples regardless sample types  }
  \item{bin.size}{ numeric value of genomic bin-size for exons}
  \item{is.verbose}{ logical value indicating if the progress information is printed}
  \item{is.plot}{ logical value indicating if the coverage summary plot is generated}
}
 
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
This function returns an object of \code{\link{PatCNVData-class}} containing per-sample coverage information generated.
}
 
\author{
  Chen Wang
}
 
%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
\code{\link{createSession}}
}
\examples{

#=== load a simulation example
config.filename <- "sim1.ini"
makeSimulation(config.filename)
sim.session <- createSession(config.filename)

#=== scan coverages of multiple samples
germline.data <- scanMultiCovg(session.name=sim.session)

#=== print BP-level coverage per sample
sampleCoverage(germline.data)

#=== print median/mean exon-coverage of selected genes
avgExonCoverage(germline.data,gene.name=c("BIK"))

}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{IO}
